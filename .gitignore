# Python
__pycache__/
*.py[cod]
*.so

# PyTorch / Lightning checkpoints & logs
*.pth
*.pt
*.ckpt
lightning_logs/
# Trained model bundles are local artifacts; keep the dir, ignore its contents.
checkpoints/*
!checkpoints/.gitkeep

# Secrets
.env

# Tooling caches
.ruff_cache/
.pytest_cache/
.mypy_cache/
# Coverage artifacts (PYQ-230 measures coverage; the data file is rebuildable).
.coverage
.coverage.*
htmlcov/

# Local data panel cache (pyquant.data.cache) -- rebuildable, not reproducible
# storage; pinned datasets live under here too but are meant to be local-only.
.cache/

# Rendered documentation (PYQ-232); the sources are checked in, the build is not.
docs/_build/
docs/api/_generated/

# Generated chart exports
exports/

# Virtual environments
.venv/
venv/
env/

# IDEs
.vscode/
.idea/
*.swp
.DS_Store

# Logs
*.log

CLAUDE.md
backlog_adds.md
review.md
systems_research.md

pyquant-agentic-loop-brief.md
img.png